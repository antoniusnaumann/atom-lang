main(n Int) Int {
    fib(n)
}

/// Calculates the nth fibonacci number
fib(nth Int) Int {
    if (nth == 0) {
        0
    } else {
        if (nth == 1) {
            1
        } else {
            fib(nth - 1) + fib(nth - 2)
        }
    }
}
