ast::*

Token (
    kind TokenKind
    span Span
)

TokenKind (
    // identifier
    TypeIdent(String)
    ValueIdent(String)
    DollarIdent(UInt)

    // literals
    IntLit(UInt(64)) // TODO: allow larger integers as well
    FloatLit(Float(64)) 
    StringLit(String)
    RuneLit(Rune)

    // operators
    Plus       // +
    Minus      // -
    Star       // *
    Slash      // /
    Percent    // %
    Assign     // =
    Lt         // <
    Gt         // >
    And        // &
    Or         // |
    Xor        // ^
    Not        // !
    Question   // ?
    Tilde      // ~
    Dot        // .
    Colon      // :
    Hash       // #

    PlusPlus   // ++
    PlusAssign // +=
    MinusAssign// -=
    StarAssign // *=
    SlashAssign// /=
    PercentAssign// %=
    AndAssign  // &=
    OrAssign   // |=
    XorAssign  // ^=
    Equal      // ==
    NotEq      // !=
    LtEq       // <=
    GtEq       // >=
    AndAnd     // &&
    OrOr       // ||
    LShift     // <<
    RShift     // >>
    LShiftAssign // <<=
    RShiftAssign // >>==
    ColonColon // ::
    Arrow      // ->
    DotDot     // ..
    PlusPlusEq // ++=

    // delimiters
    LParen     // (
    RParen     // )
    LBrace     // {
    RBrace     // }
    Comma      // ,
    Semicolon  // ;

    // special
    Newline   // in suitable positions, \n interpreted as , in struct and enum declarations and as ; in function bodies
    Invalid
)

is_continuation(token Token) Bool {
    match(token) {
        Plus | Minus | Star | Slash | Percent
        | Eq | Lt | Gt | And | Or
        | Dot | Comma | RParen | RBrace { True }
        _ { False }
    }
}
